collections:
  articles:
    output: true
    sort_by: "index"
  issues:
    output: true
defaults:
  - scope:
      type: "articles"
    values:
      layout: "article"
exclude:
  - "exclude"
plugins:
  - "jekyll-redirect-from"
whitelist:
  - "jekyll-redirect-from"
